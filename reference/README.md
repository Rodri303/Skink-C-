# Reference material

`Web-Demo-SWINK.html` is preserved only as a migration reference for Skink's previous layout and behavior.

Do not import it into the native application or build new runtime dependencies around it. Native C++/Qt modules should reproduce only the behavior we deliberately approve during migration.
